#[allow(clippy::needless_raw_string_hashes)]
#[test]
fn test_cli_usage_n{idx:03}() {{
    let mut cmd = assert_cmd::Command::cargo_bin("codi")
        .expect("binary is not found, build project first");

    cmd.args({args:?})
        .write_stdin(r##"{stdin}"##)
        .assert()
        .stdout(r##"{stdout}"##)
        .stderr(r##"{stderr}"##);
}}
